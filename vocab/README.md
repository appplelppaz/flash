# vocab/

`netflix-subs-to-vocab` スキルが書き出した語彙リストを置く場所。

ファイル名は `<lang>_<title>.tsv`（例 `es_lacasadepapel_s1.tsv`）。
アプリの取り込み画面はこの名前から言語とリスト名を読み取る。

中身はタブ区切りの 4 列・ヘッダーなし。

```
単語	日本語訳	例文	例文の日本語訳
```

`en_sample.tsv` は動きを確かめるための見本。
